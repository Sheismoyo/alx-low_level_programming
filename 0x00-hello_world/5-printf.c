#include <stdio.h>

/**
 * main - Using printf function in a c program
 * Return: Always 0 (Success)
 */
 
int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}
